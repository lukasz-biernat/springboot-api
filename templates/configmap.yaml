apiVersion: v1
kind: ConfigMap
metadata:
  name: springboot-api-config
data:
  config.json: |
    {
      "appName": "SpringBootAPI",
      "version": "1.0.0",
      "environment": "{{ .Values.envName }}" # dev or prd
    }
