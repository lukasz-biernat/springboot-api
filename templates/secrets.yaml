apiVersion: v1
kind: Secret
metadata:
  name: springboot-api-secret
type: Opaque
data:
  # Values are randomly generated using a helper function (see below)
  secret-key-1: {{ randAlphaNum 16 | b64enc }}
  secret-key-2: {{ randAlphaNum 16 | b64enc }}
